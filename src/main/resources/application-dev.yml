spring:
  datasource:
    url: jdbc:mysql://localhost:3306/seederslab?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: monica01

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${mail.username}
    password: ${mail.password}
    properties:
      mail:
        debug: true
        smtp:
          auth: true
          starttls:
            enable: true


logging:
  level:
    com.ssak.ssak: DEBUG
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

    # 예시